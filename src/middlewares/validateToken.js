export default function validateToken(schema) {
    return ((req, res, next) => {
        //usar schema para retornar erros para o front end
        //devido a falha de autenticação
        //pelo token
    });
}