export default function validateUser(schema) {
    return ((req, res, next) => {
        //usar schema para retornar erros para o usuario
        //devido aos dados inseridos
        //para a criação do usuario
    });
}