export default function validateUrl(schema) {
    return ((req, res, next) => {
        //usar schema para retornar erros para o front
        //devido aos dados inseridos
        //em relação a url
    });
}