export default function validateLogin(schema) {
    return ((req, res, next) => {
        //usar schema para retornar erros para o usuario
        //devido aos dados inseridos
        //para o login do usuario
    });
}